<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fitshield">

    <!-- Features -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/whatsapp_image_2025_02_15_at_10_32_52_c0b30c80_removebg_preview__1_"
        android:label="@string/app_name"
        android:roundIcon="@drawable/whatsapp_image_2025_02_15_at_10_32_52_c0b30c80_removebg_preview__1_"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fitshield"
        tools:targetApi="34">

        <!-- Launcher -->
        <activity
            android:name=".PermissionsActivity"
            android:exported="true"
            android:theme="@style/Theme.FitShield">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name=".HomePageActivity" android:exported="true" android:theme="@style/Theme.FitShield" />
        <activity android:name=".EmergencyAlertActivity" />
        <activity android:name=".EmergencyContactsActivity" android:exported="true" android:theme="@style/Theme.FitShield" />

        <!-- ✅ Only one service declaration is needed -->
        <service
            android:name=".FloatingIconService"
            android:enabled="true"
            android:exported="false"/>


        <!-- Boot Receiver -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
